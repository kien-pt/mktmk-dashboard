import React from "react";

const Component0 = ({ data }) => {
  return (
    <div>
      {data?.map((item) => (
        <div>
          {item}
        </div>
      ))}
    </div>
  )
}

export default Component0;
